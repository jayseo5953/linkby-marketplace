{
  "name": "linkby-marketplace",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "docker compose up",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "docker compose exec web npm run test && docker compose exec api npm run test",
    "test:smoke": "docker compose exec api sh -c 'SMOKE_DATABASE_URL=$DATABASE_URL npm run test:smoke'"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.6.2"
  }
}
